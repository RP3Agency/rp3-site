/**
 * Change "template" to a code representative of this case study.
 */

var template = (function($) {

	var

	/**
	 * Tasks to do onResize
	 */
	onResize = function() {
		/* we'll do stuff here later */
	},

	init = function() {
		/* do stuff that we define above */

		onResize();
	};

	return {
		init:init
	};

}(jQuery));

(function() {
	'use strict';
	template.init();
}());
