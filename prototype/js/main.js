;(function($) {

	'use strict';

	// $('#slide-1').waypoint( function( direction ) {
	// 	if ( direction === 'down' ) {
	// 		$(this).css( 'position', 'absolute' ).css( 'top', 0 );
	// 	} else {
	// 		$(this).css( 'position', 'relative' );
	// 	}
	// });

	// $(window).scroll(function () {
	//     if ( $(window).scrollTop() > 779 ) {
	//         $('#slide-1').css( 'top', $(window).scrollTop() ).css( 'position', 'relative' );
	//         alert( 'foobar' );
	//     }
	// });

	// $('#video-wrapper').parallax( '50%', 0.1 );
}(jQuery));
